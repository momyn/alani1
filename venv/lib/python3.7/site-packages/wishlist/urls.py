# from django.conf.urls import patterns
from django.conf.urls.static import static
from django.contrib import admin
from django.urls import path, include
from menu1 import settings
from app import views

from .views import (
    WishlistView, WishlistAddView, WishlistClearView, WishlistRemoveView
)



urlpatterns = [
    path(r'^$', WishlistView.as_view(), name='wishlist'),
    path(r'^add/$', WishlistAddView.as_view(), name='wishlist_add'),
    path(r'^clear/$', WishlistClearView.as_view(), name='wishlist_clear'),
    path(r'^(?P<pk>\d+)/remove/$', WishlistRemoveView.as_view(), name='wishlist_remove'),
]

# urlpatterns = patterns('',
#     path('$', WishlistView.as_view(), name='wishlist'),
#     path('add/$', WishlistAddView.as_view(), name='wishlist_add'),
#     path('clear/$', WishlistClearView.as_view(), name='wishlist_clear'),
#     path('(?P<pk>\d+)/remove/$', WishlistRemoveView.as_view(), name='wishlist_remove'),
# )
